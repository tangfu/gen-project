CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
#SET(PROJ_NAME xxx)
PROJECT(${PROJ_NAME})

FIND_PACKAGE(xxxx REQUIRED)

ADD_SUBDIRECTORY(src)
INSTALL(PROGRAMS build/src/${PROJ_NAME} DESTINATION ${PROJECT_SOURCE_DIR}/${PROJ_NAME}/bin)
INSTALL(DIRECTORY tools DESTINATION ${PROJECT_SOURCE_DIR}/${PROJ_NAME}) 
INSTALL(FILES ${PROJ_NAME}.conf DESTINATION ${PROJECT_SOURCE_DIR}/${PROJ_NAME}/conf)
